import HeaderComponent from "../components/HeaderComponent";
import ListComponent from "../components/ListComponent";

const RemovedPage = () => {
  return (
    <>
      <HeaderComponent />
      <ListComponent isRemoved />
    </>
  );
};

export default RemovedPage;
